# 贡献指南

感谢考虑贡献！本仓库的目标是建立**可组合、可校验**的动漫提示词规范，而不是堆砌提示词数量。

## 可以贡献什么

| 类型 | 说明 | 位置 |
|---|---|---|
| 新风格模板 | 按 Schema 写 YAML，一个模板一个文件 | `prompts/{对应分类}/` |
| 词库补充 | 新的光影/镜头/环境/情绪词条或材质词 | `data/*.json` |
| 完整案例 | 变量填充 + 三平台适配输出 | `examples/` |
| 文档改进 | 修正、翻译、补充实战经验 | `docs/` |
| 校验脚本 | 基于 JSON Schema 的自动检查工具 | `scripts/` |

## 提交新模板的硬性要求

1. **遵循 Schema**：YAML 结构必须符合 `schemas/anime-prompt.schema.json`。
2. **层级纪律**：镜头词不进 lighting；mood ≤ 2 个词且放最后；无裸情绪词。
3. **冲突自查**：对照 `docs/style-conflicts.md`，冲突信息写入 `conflicts:` 字段。
4. **负面清单必填**：从 `data/negative-prompts.json` 取通用项 + 本风格专属禁区。
5. **声明平台**：`platforms:` 只写你实际验证过或明确适配的平台。
6. **命名规范**：文件名 = 模板 id（kebab-case），如 `ink-wash-wuxia.yaml`。

## 继承规则（项目交接约定）

- 不擅自删除既有的人物固定特征、画风锚点、负面词、已验证参数。
- 修改与既有锚点冲突时：先在 PR 中说明冲突，同时提供**保守兼容版**和**风格强化版**。
- 未被指定修改的部分，默认继承最终确认版；多个版本并存时以后提交者为准。

## 版权红线（违反将直接关闭 PR）

- ❌ 不上传他人受版权保护的原图或未授权的角色图。
- ❌ 不做"模仿某在世画师/工作室精确风格"的模板；改用视觉特征描述（如"柔和水彩、细腻赛璐璐"）。
- ❌ 不直接搬运他人完整提示词而不注明出处。
- ✅ 社区案例须在 `credits:` 字段说明来源、作者、授权状态及是否允许商用。
- ✅ 引用第三方公开案例时保留来源链接；权利人可通过 Issue 要求移除。

## PR 流程

1. Fork → 新建分支 `feat/your-template-id`
2. 一个 PR 聚焦一类变更
3. 自查上方硬性要求逐条通过
4. PR 描述中附：目标平台、画幅、一句话效果描述

## License

贡献内容默认以 [MIT](LICENSE) 随仓库开源。含第三方素材的贡献必须单独声明授权状态。
